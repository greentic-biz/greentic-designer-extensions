# Phase 1 Track A — Scaffolding (`gtdx new`) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add `gtdx new <NAME>` subcommand that scaffolds a compilable Greentic Designer Extension project (design/bundle/deploy) using WIT contract embedded in the `gtdx` binary, with a `.gtdx-contract.lock` file tracking vendored WIT hashes.

**Architecture:** Follow the existing `src/commands/<name>.rs` pattern. Add a sibling `src/scaffold/` module that contains template rendering, preflight checks, embedded-WIT access, and contract-lock writing. Templates live in `crates/greentic-ext-cli/templates/` as raw `.tmpl` files embedded via `include_dir!`. A build script (`build.rs`) copies `wit/*.wit` into `crates/greentic-ext-cli/embedded-wit/<version>/` at build time so the binary always ships with the current contract.

**Tech Stack:** Rust 1.94, edition 2024, clap, dialoguer, anyhow, tempfile, sha2, toml, include_dir, anstyle.

**Spec:** `docs/superpowers/specs/2026-04-18-phase1-scaffolding-design.md`
**Parent:** `docs/superpowers/specs/2026-04-18-dx-10-10-roadmap.md` (subsystem S1)

---

## File Structure

### Create

- `crates/greentic-ext-cli/build.rs` — copy `wit/*.wit` → `crates/greentic-ext-cli/embedded-wit/<version>/` at build time.
- `crates/greentic-ext-cli/src/commands/new.rs` (~250 LOC) — command entry, args, orchestration.
- `crates/greentic-ext-cli/src/scaffold/mod.rs` (~50 LOC) — module root.
- `crates/greentic-ext-cli/src/scaffold/template.rs` (~200 LOC) — placeholder substitution + file writing.
- `crates/greentic-ext-cli/src/scaffold/preflight.rs` (~150 LOC) — toolchain + dir checks.
- `crates/greentic-ext-cli/src/scaffold/embedded.rs` (~80 LOC) — `include_dir!` wrappers + SHA256.
- `crates/greentic-ext-cli/src/scaffold/contract_lock.rs` (~100 LOC) — `.gtdx-contract.lock` writer.
- `crates/greentic-ext-cli/templates/common/gitignore.tmpl`
- `crates/greentic-ext-cli/templates/common/README.md.tmpl`
- `crates/greentic-ext-cli/templates/common/build.sh.tmpl`
- `crates/greentic-ext-cli/templates/common/ci/local_check.sh.tmpl`
- `crates/greentic-ext-cli/templates/common/i18n/en.json.tmpl`
- `crates/greentic-ext-cli/templates/design/Cargo.toml.tmpl`
- `crates/greentic-ext-cli/templates/design/describe.json.tmpl`
- `crates/greentic-ext-cli/templates/design/src/lib.rs.tmpl`
- `crates/greentic-ext-cli/templates/design/wit/world.wit.tmpl`
- `crates/greentic-ext-cli/templates/design/prompts/system.md.tmpl`
- `crates/greentic-ext-cli/templates/bundle/Cargo.toml.tmpl`
- `crates/greentic-ext-cli/templates/bundle/describe.json.tmpl`
- `crates/greentic-ext-cli/templates/bundle/src/lib.rs.tmpl`
- `crates/greentic-ext-cli/templates/bundle/wit/world.wit.tmpl`
- `crates/greentic-ext-cli/templates/deploy/Cargo.toml.tmpl`
- `crates/greentic-ext-cli/templates/deploy/describe.json.tmpl`
- `crates/greentic-ext-cli/templates/deploy/src/lib.rs.tmpl`
- `crates/greentic-ext-cli/templates/deploy/wit/world.wit.tmpl`
- `crates/greentic-ext-cli/tests/cli_new.rs` — integration tests (spawn binary, verify output).

### Modify

- `Cargo.toml` (workspace root) — add `include_dir = "0.7"`, `toml = "0.8"`, `anstyle = "1"`, `which = "6"`.
- `crates/greentic-ext-cli/Cargo.toml` — add deps (`include_dir`, `toml`, `anstyle`, `sha2`, `semver`, `which`), add `build-dependencies` block.
- `crates/greentic-ext-cli/src/main.rs` — register `New` variant in `Command` enum, route to `commands::new::run`.
- `crates/greentic-ext-cli/src/commands/mod.rs` — add `pub mod new;`.
- `crates/greentic-ext-cli/src/lib.rs` if exists, else `main.rs` — declare `mod scaffold;`.

### Ignore (gitignore)

- `crates/greentic-ext-cli/embedded-wit/` — generated by `build.rs`; never committed.

---

## Task 1: Add workspace dependencies

**Files:**
- Modify: `Cargo.toml`
- Modify: `crates/greentic-ext-cli/Cargo.toml`

- [ ] **Step 1: Add workspace deps**

Edit `Cargo.toml` (root), add to `[workspace.dependencies]`:

```toml
include_dir = "0.7"
toml = "0.8"
anstyle = "1"
which = "6"
```

- [ ] **Step 2: Add to CLI crate deps**

Edit `crates/greentic-ext-cli/Cargo.toml`:

```toml
[dependencies]
# ... existing deps ...
anstyle = { workspace = true }
include_dir = { workspace = true }
sha2 = { workspace = true }
semver = { workspace = true }
toml = { workspace = true }
which = { workspace = true }

[build-dependencies]
anyhow = { workspace = true }
```

- [ ] **Step 3: Ignore generated embedded-wit dir**

Add to `crates/greentic-ext-cli/.gitignore` (create if missing):

```
/embedded-wit/
```

- [ ] **Step 4: Verify workspace resolves**

Run: `cargo metadata --format-version 1 > /dev/null`
Expected: exit 0, no errors.

- [ ] **Step 5: Commit**

```bash
git add Cargo.toml crates/greentic-ext-cli/Cargo.toml crates/greentic-ext-cli/.gitignore
git commit -m "chore(ext-cli): add scaffolding deps (include_dir, toml, anstyle, sha2, semver, which)"
```

---

## Task 2: Build script — copy WIT contract into binary

**Files:**
- Create: `crates/greentic-ext-cli/build.rs`

- [ ] **Step 1: Write build.rs**

```rust
use std::{fs, path::Path};

fn main() -> anyhow::Result<()> {
    let version = env!("CARGO_PKG_VERSION");
    let manifest_dir = env!("CARGO_MANIFEST_DIR");
    let workspace_root = Path::new(manifest_dir)
        .parent()
        .and_then(Path::parent)
        .expect("crate is under workspace root");
    let src_dir = workspace_root.join("wit");
    let dst_dir = Path::new(manifest_dir)
        .join("embedded-wit")
        .join(version);

    if dst_dir.exists() {
        fs::remove_dir_all(&dst_dir)?;
    }
    fs::create_dir_all(&dst_dir)?;

    let mut count = 0usize;
    for entry in fs::read_dir(&src_dir)? {
        let entry = entry?;
        let path = entry.path();
        if path.extension().and_then(|e| e.to_str()) == Some("wit") {
            let name = path.file_name().expect("wit file has name");
            fs::copy(&path, dst_dir.join(name))?;
            println!("cargo:rerun-if-changed={}", path.display());
            count += 1;
        }
    }

    println!("cargo:rerun-if-changed={}", src_dir.display());
    println!("cargo:warning=embedded {} WIT files at version {}", count, version);
    Ok(())
}
```

- [ ] **Step 2: Run build, verify embedded-wit appears**

Run: `cargo build -p greentic-ext-cli 2>&1 | tail -20`
Expected: build succeeds. Check `ls crates/greentic-ext-cli/embedded-wit/0.1.0/`:
```
extension-base.wit
extension-bundle.wit
extension-deploy.wit
extension-design.wit
extension-host.wit
runtime-side.wit
```

- [ ] **Step 3: Commit**

```bash
git add crates/greentic-ext-cli/build.rs
git commit -m "feat(ext-cli): build.rs copies wit/ into embedded-wit/<version>/ for binary embedding"
```

---

## Task 3: Scaffold module skeleton + wire into CLI

**Files:**
- Create: `crates/greentic-ext-cli/src/scaffold/mod.rs`
- Create: `crates/greentic-ext-cli/src/commands/new.rs`
- Modify: `crates/greentic-ext-cli/src/commands/mod.rs`
- Modify: `crates/greentic-ext-cli/src/main.rs`

- [ ] **Step 1: Create scaffold module root**

`crates/greentic-ext-cli/src/scaffold/mod.rs`:

```rust
//! Scaffolding logic for `gtdx new`.

pub mod contract_lock;
pub mod embedded;
pub mod preflight;
pub mod template;

/// Extension kinds that can be scaffolded.
#[derive(Debug, Clone, Copy, PartialEq, Eq, clap::ValueEnum)]
pub enum Kind {
    Design,
    Bundle,
    Deploy,
}

impl Kind {
    pub fn as_str(self) -> &'static str {
        match self {
            Kind::Design => "design",
            Kind::Bundle => "bundle",
            Kind::Deploy => "deploy",
        }
    }
}
```

- [ ] **Step 2: Create empty submodules so scaffold/mod.rs compiles**

`crates/greentic-ext-cli/src/scaffold/contract_lock.rs`:
```rust
//! Writer for .gtdx-contract.lock files.
```

`crates/greentic-ext-cli/src/scaffold/embedded.rs`:
```rust
//! Embedded WIT resources accessor.
```

`crates/greentic-ext-cli/src/scaffold/preflight.rs`:
```rust
//! Preflight checks before scaffolding.
```

`crates/greentic-ext-cli/src/scaffold/template.rs`:
```rust
//! Template rendering and file writing.
```

- [ ] **Step 3: Create commands/new.rs with minimal Args + stub run**

`crates/greentic-ext-cli/src/commands/new.rs`:

```rust
use std::path::{Path, PathBuf};

use clap::Args as ClapArgs;

use crate::scaffold::Kind;

#[derive(ClapArgs, Debug)]
pub struct Args {
    /// Project folder name (kebab-case). Also default id suffix.
    pub name: String,

    /// Extension kind
    #[arg(short = 'k', long, value_enum, default_value = "design")]
    pub kind: Kind,

    /// Extension id (reverse-DNS). Default: com.example.<name>
    #[arg(short = 'i', long)]
    pub id: Option<String>,

    /// Initial version
    #[arg(short = 'v', long, default_value = "0.1.0")]
    pub version: String,

    /// Author name; defaults to git config user.name
    #[arg(long)]
    pub author: Option<String>,

    /// SPDX license id
    #[arg(long, default_value = "Apache-2.0")]
    pub license: String,

    /// Skip `git init`
    #[arg(long)]
    pub no_git: bool,

    /// Output directory; defaults to ./<name>
    #[arg(long)]
    pub dir: Option<PathBuf>,

    /// Overwrite if target exists
    #[arg(long)]
    pub force: bool,

    /// Skip interactive prompts
    #[arg(short = 'y', long)]
    pub yes: bool,
}

pub fn run(args: &Args, _home: &Path) -> anyhow::Result<()> {
    anyhow::bail!(
        "gtdx new is not yet implemented (name={}, kind={:?})",
        args.name,
        args.kind
    )
}
```

- [ ] **Step 4: Register `new` in commands/mod.rs**

Edit `crates/greentic-ext-cli/src/commands/mod.rs`, add to the `pub mod` block (sorted):

```rust
pub mod new;
```

- [ ] **Step 5: Wire into main.rs**

Edit `crates/greentic-ext-cli/src/main.rs`:

1. Add `mod scaffold;` at top (after `mod commands;`).
2. Add variant to `Command` enum (alphabetical-ish, near `Info`):
   ```rust
       /// Scaffold a new extension project
       New(commands::new::Args),
   ```
3. Add match arm in `main()`:
   ```rust
           Command::New(args) => commands::new::run(&args, &home),
   ```

- [ ] **Step 6: Verify build + `gtdx new --help` shows**

Run: `cargo build -p greentic-ext-cli && ./target/debug/gtdx new --help`
Expected: help text lists `<NAME>`, `--kind`, `--id`, `--version`, etc.

- [ ] **Step 7: Verify stub runs (and errors)**

Run: `./target/debug/gtdx new demo 2>&1 | head -3`
Expected: `Error: gtdx new is not yet implemented (name=demo, kind=Design)` exit 1.

- [ ] **Step 8: Commit**

```bash
git add crates/greentic-ext-cli/src/scaffold crates/greentic-ext-cli/src/commands/new.rs crates/greentic-ext-cli/src/commands/mod.rs crates/greentic-ext-cli/src/main.rs
git commit -m "feat(ext-cli): scaffold module + gtdx new command skeleton (stub impl)"
```

---

## Task 4: Embedded WIT accessor — directory listing

**Files:**
- Modify: `crates/greentic-ext-cli/src/scaffold/embedded.rs`

- [ ] **Step 1: Write failing test**

Append to `crates/greentic-ext-cli/src/scaffold/embedded.rs`:

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn wit_files_returns_all_embedded_packages() {
        let files = wit_files();
        assert!(files.iter().any(|f| f.name == "extension-base.wit"));
        assert!(files.iter().any(|f| f.name == "extension-host.wit"));
        assert!(files.iter().any(|f| f.name == "extension-design.wit"));
        assert!(files.iter().any(|f| f.name == "extension-bundle.wit"));
        assert!(files.iter().any(|f| f.name == "extension-deploy.wit"));
        assert_eq!(files.len(), 6); // 5 packages + runtime-side
    }
}
```

- [ ] **Step 2: Run test, verify it fails**

Run: `cargo test -p greentic-ext-cli --lib scaffold::embedded::tests::wit_files_returns_all_embedded_packages 2>&1 | tail -5`
Expected: FAIL — `wit_files` not found.

- [ ] **Step 3: Implement wit_files()**

Replace body of `crates/greentic-ext-cli/src/scaffold/embedded.rs`:

```rust
//! Embedded WIT resources accessor.

use include_dir::{include_dir, Dir};

pub const CONTRACT_VERSION: &str = env!("CARGO_PKG_VERSION");

static EMBEDDED: Dir<'_> =
    include_dir!("$CARGO_MANIFEST_DIR/embedded-wit/$CARGO_PKG_VERSION");

pub struct WitFile {
    pub name: &'static str,
    pub bytes: &'static [u8],
}

pub fn wit_files() -> Vec<WitFile> {
    EMBEDDED
        .files()
        .map(|f| WitFile {
            name: f
                .path()
                .file_name()
                .and_then(|s| s.to_str())
                .expect("embedded wit filename"),
            bytes: f.contents(),
        })
        .collect()
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn wit_files_returns_all_embedded_packages() {
        let files = wit_files();
        assert!(files.iter().any(|f| f.name == "extension-base.wit"));
        assert!(files.iter().any(|f| f.name == "extension-host.wit"));
        assert!(files.iter().any(|f| f.name == "extension-design.wit"));
        assert!(files.iter().any(|f| f.name == "extension-bundle.wit"));
        assert!(files.iter().any(|f| f.name == "extension-deploy.wit"));
        assert_eq!(files.len(), 6);
    }
}
```

- [ ] **Step 4: Run test, verify pass**

Run: `cargo test -p greentic-ext-cli --lib scaffold::embedded 2>&1 | tail -5`
Expected: 1 passed.

- [ ] **Step 5: Commit**

```bash
git add crates/greentic-ext-cli/src/scaffold/embedded.rs
git commit -m "feat(ext-cli): embedded WIT accessor via include_dir!"
```

---

## Task 5: Embedded WIT accessor — per-kind filter + SHA256 helper

**Files:**
- Modify: `crates/greentic-ext-cli/src/scaffold/embedded.rs`

- [ ] **Step 1: Write failing tests**

Replace the `tests` mod in `embedded.rs`:

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn wit_files_returns_all_embedded_packages() {
        let files = wit_files();
        assert_eq!(files.len(), 6);
    }

    #[test]
    fn files_for_kind_design_includes_base_host_and_design() {
        let files = files_for_kind("design");
        let names: Vec<_> = files.iter().map(|f| f.name).collect();
        assert!(names.contains(&"extension-base.wit"));
        assert!(names.contains(&"extension-host.wit"));
        assert!(names.contains(&"extension-design.wit"));
        assert!(!names.contains(&"extension-bundle.wit"));
    }

    #[test]
    fn files_for_kind_bundle_includes_bundle_not_design() {
        let files = files_for_kind("bundle");
        let names: Vec<_> = files.iter().map(|f| f.name).collect();
        assert!(names.contains(&"extension-bundle.wit"));
        assert!(!names.contains(&"extension-design.wit"));
    }

    #[test]
    fn sha256_hex_is_deterministic() {
        let a = sha256_hex(b"hello");
        let b = sha256_hex(b"hello");
        assert_eq!(a, b);
        assert_eq!(a.len(), 64);
    }
}
```

- [ ] **Step 2: Run tests, verify they fail**

Run: `cargo test -p greentic-ext-cli --lib scaffold::embedded 2>&1 | tail -10`
Expected: 3 FAIL (`files_for_kind` and `sha256_hex` not defined).

- [ ] **Step 3: Add implementations**

Append to `embedded.rs` (above `#[cfg(test)]`):

```rust
/// Returns the subset of WIT files needed to scaffold an extension of the given kind.
/// Always includes `extension-base.wit` and `extension-host.wit`.
pub fn files_for_kind(kind: &str) -> Vec<WitFile> {
    let kind_file = format!("extension-{kind}.wit");
    wit_files()
        .into_iter()
        .filter(|f| {
            matches!(f.name, "extension-base.wit" | "extension-host.wit") || f.name == kind_file
        })
        .collect()
}

pub fn sha256_hex(bytes: &[u8]) -> String {
    use sha2::{Digest, Sha256};
    let digest = Sha256::digest(bytes);
    let mut out = String::with_capacity(64);
    for byte in digest {
        use std::fmt::Write as _;
        write!(&mut out, "{byte:02x}").expect("write to string");
    }
    out
}
```

- [ ] **Step 4: Run tests, verify pass**

Run: `cargo test -p greentic-ext-cli --lib scaffold::embedded 2>&1 | tail -10`
Expected: 4 passed.

- [ ] **Step 5: Commit**

```bash
git add crates/greentic-ext-cli/src/scaffold/embedded.rs
git commit -m "feat(ext-cli): per-kind WIT filter + sha256 helper for contract lock"
```

---

## Task 6: Contract lock writer

**Files:**
- Modify: `crates/greentic-ext-cli/src/scaffold/contract_lock.rs`

- [ ] **Step 1: Write failing test**

Replace `contract_lock.rs`:

```rust
//! Writer for .gtdx-contract.lock files.

use std::collections::BTreeMap;

use serde::Serialize;

#[derive(Debug, Serialize)]
pub struct ContractLock {
    pub contract_version: String,
    pub generated_by: String,
    pub generated_at: String,
    pub files: BTreeMap<String, String>,
}

impl ContractLock {
    pub fn to_toml(&self) -> anyhow::Result<String> {
        toml::to_string_pretty(self).map_err(Into::into)
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn lock_serializes_with_files_table() {
        let mut files = BTreeMap::new();
        files.insert(
            "wit/deps/greentic/extension-base/world.wit".to_string(),
            "sha256:abc".to_string(),
        );
        let lock = ContractLock {
            contract_version: "0.1.0".to_string(),
            generated_by: "gtdx 0.1.0".to_string(),
            generated_at: "2026-04-18T00:00:00Z".to_string(),
            files,
        };
        let out = lock.to_toml().expect("serialize");
        assert!(out.contains("contract_version = \"0.1.0\""));
        assert!(out.contains("[files]"));
        assert!(out.contains("wit/deps/greentic/extension-base/world.wit"));
    }

    #[test]
    fn lock_file_ordering_is_deterministic() {
        let mut files = BTreeMap::new();
        files.insert("z.wit".to_string(), "z".to_string());
        files.insert("a.wit".to_string(), "a".to_string());
        let lock = ContractLock {
            contract_version: "0.1.0".to_string(),
            generated_by: "gtdx".to_string(),
            generated_at: "now".to_string(),
            files,
        };
        let out = lock.to_toml().unwrap();
        let a_pos = out.find("a.wit").unwrap();
        let z_pos = out.find("z.wit").unwrap();
        assert!(a_pos < z_pos, "files must be serialized in BTreeMap order");
    }
}
```

- [ ] **Step 2: Run tests, verify they fail**

Run: `cargo test -p greentic-ext-cli --lib scaffold::contract_lock 2>&1 | tail -10`
Expected: FAIL — `serde` derive path missing or serialization error (first run establishes green).

- [ ] **Step 3: Run tests again, expect pass**

Run: `cargo test -p greentic-ext-cli --lib scaffold::contract_lock 2>&1 | tail -10`
Expected: 2 passed (BTreeMap serialization is inherently sorted).

- [ ] **Step 4: Commit**

```bash
git add crates/greentic-ext-cli/src/scaffold/contract_lock.rs
git commit -m "feat(ext-cli): .gtdx-contract.lock TOML writer with deterministic file ordering"
```

---

## Task 7: Template placeholder substitution

**Files:**
- Modify: `crates/greentic-ext-cli/src/scaffold/template.rs`

- [ ] **Step 1: Write failing tests**

Replace `template.rs`:

```rust
//! Template rendering and file writing.

use std::collections::HashMap;

pub struct Context {
    values: HashMap<&'static str, String>,
}

impl Context {
    pub fn new() -> Self {
        Self {
            values: HashMap::new(),
        }
    }

    pub fn set(&mut self, key: &'static str, value: impl Into<String>) -> &mut Self {
        self.values.insert(key, value.into());
        self
    }

    pub fn render(&self, template: &str) -> anyhow::Result<String> {
        let mut out = template.to_string();
        let mut remaining_passes = 4;
        while remaining_passes > 0 {
            let before = out.clone();
            for (key, value) in &self.values {
                let token = format!("{{{{{key}}}}}");
                out = out.replace(&token, value);
            }
            if out == before {
                break;
            }
            remaining_passes -= 1;
        }
        if let Some(pos) = out.find("{{") {
            let end = out[pos..].find("}}").map(|e| pos + e + 2).unwrap_or(out.len());
            anyhow::bail!("unsubstituted placeholder: {}", &out[pos..end]);
        }
        Ok(out)
    }
}

impl Default for Context {
    fn default() -> Self {
        Self::new()
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn render_substitutes_placeholder() {
        let mut ctx = Context::new();
        ctx.set("name", "demo");
        let out = ctx.render("hello {{name}}!").unwrap();
        assert_eq!(out, "hello demo!");
    }

    #[test]
    fn render_multiple_placeholders() {
        let mut ctx = Context::new();
        ctx.set("name", "demo").set("version", "0.1.0");
        let out = ctx.render("{{name}}@{{version}}").unwrap();
        assert_eq!(out, "demo@0.1.0");
    }

    #[test]
    fn render_unsubstituted_placeholder_errors() {
        let ctx = Context::new();
        let err = ctx.render("hello {{missing}}").unwrap_err();
        assert!(err.to_string().contains("{{missing}}"));
    }

    #[test]
    fn render_literal_text_passthrough() {
        let ctx = Context::new();
        let out = ctx.render("plain text no braces").unwrap();
        assert_eq!(out, "plain text no braces");
    }
}
```

- [ ] **Step 2: Run tests, verify pass**

Run: `cargo test -p greentic-ext-cli --lib scaffold::template 2>&1 | tail -10`
Expected: 4 passed.

- [ ] **Step 3: Commit**

```bash
git add crates/greentic-ext-cli/src/scaffold/template.rs
git commit -m "feat(ext-cli): template Context with {{placeholder}} substitution and unresolved-token detection"
```

---

## Task 8: Preflight — cargo availability check

**Files:**
- Modify: `crates/greentic-ext-cli/src/scaffold/preflight.rs`

- [ ] **Step 1: Write failing tests**

Replace `preflight.rs`:

```rust
//! Preflight checks before scaffolding.

use std::path::Path;

#[derive(Debug, PartialEq, Eq)]
pub enum Check {
    Pass { name: String, detail: String },
    Warn { name: String, hint: String },
    Fail { name: String, hint: String },
}

pub fn check_cargo_available() -> Check {
    match which::which("cargo") {
        Ok(path) => Check::Pass {
            name: "cargo".into(),
            detail: path.display().to_string(),
        },
        Err(_) => Check::Fail {
            name: "cargo".into(),
            hint: "install Rust toolchain from https://rustup.rs/".into(),
        },
    }
}

pub fn check_cargo_component_available() -> Check {
    match which::which("cargo-component") {
        Ok(path) => Check::Pass {
            name: "cargo-component".into(),
            detail: path.display().to_string(),
        },
        Err(_) => Check::Warn {
            name: "cargo-component".into(),
            hint: "install with: cargo install --locked cargo-component".into(),
        },
    }
}

pub fn check_target_dir(path: &Path, force: bool) -> Check {
    if !path.exists() {
        return Check::Pass {
            name: "target directory".into(),
            detail: format!("{} (will be created)", path.display()),
        };
    }
    match std::fs::read_dir(path) {
        Ok(mut entries) => {
            if entries.next().is_none() {
                Check::Pass {
                    name: "target directory".into(),
                    detail: format!("{} (empty)", path.display()),
                }
            } else if force {
                Check::Warn {
                    name: "target directory".into(),
                    hint: format!(
                        "{} is not empty; --force will overwrite existing files",
                        path.display()
                    ),
                }
            } else {
                Check::Fail {
                    name: "target directory".into(),
                    hint: format!(
                        "{} already exists and is not empty; pass --force to overwrite",
                        path.display()
                    ),
                }
            }
        }
        Err(e) => Check::Fail {
            name: "target directory".into(),
            hint: format!("cannot read {}: {}", path.display(), e),
        },
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn cargo_check_is_pass_on_this_dev_env() {
        // This test assumes cargo is on PATH (it has to be to run cargo test).
        match check_cargo_available() {
            Check::Pass { .. } => {}
            other => panic!("expected Pass, got {other:?}"),
        }
    }

    #[test]
    fn target_dir_missing_is_pass() {
        let tmp = tempfile::tempdir().unwrap();
        let missing = tmp.path().join("nope");
        match check_target_dir(&missing, false) {
            Check::Pass { .. } => {}
            other => panic!("expected Pass, got {other:?}"),
        }
    }

    #[test]
    fn target_dir_empty_is_pass() {
        let tmp = tempfile::tempdir().unwrap();
        match check_target_dir(tmp.path(), false) {
            Check::Pass { .. } => {}
            other => panic!("expected Pass, got {other:?}"),
        }
    }

    #[test]
    fn target_dir_nonempty_no_force_is_fail() {
        let tmp = tempfile::tempdir().unwrap();
        std::fs::write(tmp.path().join("something"), "x").unwrap();
        match check_target_dir(tmp.path(), false) {
            Check::Fail { hint, .. } => {
                assert!(hint.contains("--force"));
            }
            other => panic!("expected Fail, got {other:?}"),
        }
    }

    #[test]
    fn target_dir_nonempty_with_force_is_warn() {
        let tmp = tempfile::tempdir().unwrap();
        std::fs::write(tmp.path().join("something"), "x").unwrap();
        match check_target_dir(tmp.path(), true) {
            Check::Warn { .. } => {}
            other => panic!("expected Warn, got {other:?}"),
        }
    }
}
```

- [ ] **Step 2: Run tests, verify pass**

Run: `cargo test -p greentic-ext-cli --lib scaffold::preflight 2>&1 | tail -10`
Expected: 5 passed.

- [ ] **Step 3: Commit**

```bash
git add crates/greentic-ext-cli/src/scaffold/preflight.rs
git commit -m "feat(ext-cli): preflight checks for cargo, cargo-component, target dir"
```

---

## Task 9: Preflight — wasm32-wasip2 target check

**Files:**
- Modify: `crates/greentic-ext-cli/src/scaffold/preflight.rs`

- [ ] **Step 1: Add failing test**

Append to `preflight.rs` inside the `tests` module:

```rust
    #[test]
    fn wasm_target_check_returns_some_variant() {
        let out = check_wasm32_wasip2_target();
        assert!(matches!(out, Check::Pass { .. } | Check::Warn { .. }));
    }
```

- [ ] **Step 2: Run test, verify it fails**

Run: `cargo test -p greentic-ext-cli --lib scaffold::preflight::tests::wasm_target_check_returns_some_variant 2>&1 | tail -5`
Expected: FAIL — `check_wasm32_wasip2_target` not defined.

- [ ] **Step 3: Implement**

Add to `preflight.rs` (above `#[cfg(test)]`):

```rust
pub fn check_wasm32_wasip2_target() -> Check {
    let output = std::process::Command::new("rustup")
        .args(["target", "list", "--installed"])
        .output();
    match output {
        Ok(o) if o.status.success() => {
            let list = String::from_utf8_lossy(&o.stdout);
            if list.lines().any(|l| l.trim() == "wasm32-wasip2") {
                Check::Pass {
                    name: "wasm32-wasip2 target".into(),
                    detail: "installed".into(),
                }
            } else {
                Check::Warn {
                    name: "wasm32-wasip2 target".into(),
                    hint: "install with: rustup target add wasm32-wasip2".into(),
                }
            }
        }
        _ => Check::Warn {
            name: "wasm32-wasip2 target".into(),
            hint: "rustup not available; install wasm32-wasip2 manually before first build".into(),
        },
    }
}
```

- [ ] **Step 4: Run test, verify pass**

Run: `cargo test -p greentic-ext-cli --lib scaffold::preflight 2>&1 | tail -10`
Expected: 6 passed (all previous + new).

- [ ] **Step 5: Commit**

```bash
git add crates/greentic-ext-cli/src/scaffold/preflight.rs
git commit -m "feat(ext-cli): preflight check for wasm32-wasip2 rustup target"
```

---

## Task 10: Template file writer (write templated file tree to disk)

**Files:**
- Modify: `crates/greentic-ext-cli/src/scaffold/template.rs`

- [ ] **Step 1: Add failing test**

Append to `template.rs` inside `tests` module:

```rust
    #[test]
    fn write_file_creates_parent_dirs() {
        let tmp = tempfile::tempdir().unwrap();
        let dst = tmp.path().join("a/b/c/file.txt");
        write_file(&dst, b"hello").unwrap();
        assert_eq!(std::fs::read(&dst).unwrap(), b"hello");
    }

    #[test]
    fn render_and_write_substitutes_before_writing() {
        let tmp = tempfile::tempdir().unwrap();
        let dst = tmp.path().join("out.txt");
        let mut ctx = Context::new();
        ctx.set("who", "world");
        render_and_write(&ctx, "hello {{who}}", &dst).unwrap();
        assert_eq!(std::fs::read_to_string(&dst).unwrap(), "hello world");
    }
```

- [ ] **Step 2: Run tests, verify fail**

Run: `cargo test -p greentic-ext-cli --lib scaffold::template 2>&1 | tail -5`
Expected: FAIL — `write_file` / `render_and_write` not defined.

- [ ] **Step 3: Implement**

Add to `template.rs` (above `#[cfg(test)]`):

```rust
use std::{fs, path::Path};

pub fn write_file(path: &Path, bytes: &[u8]) -> anyhow::Result<()> {
    if let Some(parent) = path.parent() {
        fs::create_dir_all(parent)?;
    }
    fs::write(path, bytes)?;
    Ok(())
}

pub fn render_and_write(ctx: &Context, template: &str, path: &Path) -> anyhow::Result<()> {
    let rendered = ctx.render(template)?;
    write_file(path, rendered.as_bytes())
}
```

- [ ] **Step 4: Run tests, verify pass**

Run: `cargo test -p greentic-ext-cli --lib scaffold::template 2>&1 | tail -10`
Expected: 6 passed.

- [ ] **Step 5: Commit**

```bash
git add crates/greentic-ext-cli/src/scaffold/template.rs
git commit -m "feat(ext-cli): template write_file + render_and_write helpers"
```

---

## Task 11: Common template assets

**Files:**
- Create: `crates/greentic-ext-cli/templates/common/gitignore.tmpl`
- Create: `crates/greentic-ext-cli/templates/common/README.md.tmpl`
- Create: `crates/greentic-ext-cli/templates/common/build.sh.tmpl`
- Create: `crates/greentic-ext-cli/templates/common/ci/local_check.sh.tmpl`
- Create: `crates/greentic-ext-cli/templates/common/i18n/en.json.tmpl`

- [ ] **Step 1: Write gitignore**

`crates/greentic-ext-cli/templates/common/gitignore.tmpl`:

```
/target
/dist
*.gtxpack
.DS_Store
```

- [ ] **Step 2: Write README template**

`crates/greentic-ext-cli/templates/common/README.md.tmpl`:

```markdown
# {{name}}

A Greentic Designer **{{kind}}** extension.

- id: `{{id}}`
- version: `{{version}}`
- contract: `greentic:extension-{{kind}}@{{contract_version}}`

## Develop

```
gtdx dev           # watch, rebuild, and reinstall to local registry on save
```

## Publish

```
gtdx publish       # produce dist/{{id}}-{{version}}.gtxpack + install to local registry
```

## Layout

- `describe.json` — extension manifest
- `src/lib.rs`    — WASM guest exports
- `wit/`          — WIT contract (vendored by `gtdx new`; see `.gtdx-contract.lock`)
- `i18n/en.json`  — user-facing strings
```

- [ ] **Step 3: Write build.sh template**

`crates/greentic-ext-cli/templates/common/build.sh.tmpl`:

```bash
#!/usr/bin/env bash
set -euo pipefail
cargo component build --release
mkdir -p dist
cd target/wasm32-wasip2/release
# Additional packaging done by `gtdx publish`; this script just builds the wasm.
ls -lh *.wasm
```

- [ ] **Step 4: Write ci/local_check.sh template**

`crates/greentic-ext-cli/templates/common/ci/local_check.sh.tmpl`:

```bash
#!/usr/bin/env bash
set -euo pipefail
cargo fmt --all -- --check
cargo clippy --all-targets -- -D warnings
cargo test
./build.sh
```

- [ ] **Step 5: Write i18n template**

`crates/greentic-ext-cli/templates/common/i18n/en.json.tmpl`:

```json
{
  "extension.name": "{{name}}",
  "extension.description": "A Greentic Designer {{kind}} extension."
}
```

- [ ] **Step 6: Commit**

```bash
git add crates/greentic-ext-cli/templates/common
git commit -m "feat(ext-cli): common scaffold templates (gitignore, README, build.sh, ci, i18n)"
```

---

## Task 12: Design kind templates

**Files:**
- Create: `crates/greentic-ext-cli/templates/design/Cargo.toml.tmpl`
- Create: `crates/greentic-ext-cli/templates/design/describe.json.tmpl`
- Create: `crates/greentic-ext-cli/templates/design/src/lib.rs.tmpl`
- Create: `crates/greentic-ext-cli/templates/design/wit/world.wit.tmpl`
- Create: `crates/greentic-ext-cli/templates/design/prompts/system.md.tmpl`

- [ ] **Step 1: Cargo.toml**

`crates/greentic-ext-cli/templates/design/Cargo.toml.tmpl`:

```toml
[package]
name = "{{name}}"
version = "{{version}}"
edition = "2024"
license = "{{license}}"
authors = ["{{author}}"]

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen-rt = { version = "0.35", features = ["bitflags"] }

[package.metadata.component]
package = "{{id_wit}}"

[package.metadata.component.target]
path = "wit"

[package.metadata.component.target.dependencies]
"greentic:extension-base" = { path = "wit/deps/greentic/extension-base" }
"greentic:extension-host" = { path = "wit/deps/greentic/extension-host" }
"greentic:extension-design" = { path = "wit/deps/greentic/extension-design" }
```

- [ ] **Step 2: describe.json**

`crates/greentic-ext-cli/templates/design/describe.json.tmpl`:

```json
{
  "$schema": "https://store.greentic.ai/schemas/describe-v1.json",
  "apiVersion": "greentic.ai/v1",
  "kind": "design",
  "metadata": {
    "id": "{{id}}",
    "name": "{{name}}",
    "version": "{{version}}",
    "description": "A Greentic Designer design extension.",
    "authors": ["{{author}}"],
    "license": "{{license}}"
  },
  "engine": {
    "contract": "greentic:extension-design@{{contract_version}}"
  },
  "capabilities": {
    "offered": [],
    "required": []
  },
  "permissions": {
    "network": [],
    "secrets": [],
    "callExtensionKinds": []
  },
  "runtime": {
    "component": "extension.wasm"
  }
}
```

- [ ] **Step 3: src/lib.rs**

`crates/greentic-ext-cli/templates/design/src/lib.rs.tmpl`:

```rust
// Design extension guest for {{id}}.
// Edit `validate_flow_node` to implement your authoring logic.

wit_bindgen::generate!({
    world: "extension",
    path: "wit",
});

struct Component;

impl exports::greentic::extension_design::design::Guest for Component {
    fn validate_flow_node(_input: String) -> Result<String, String> {
        // TODO: implement. Return JSON string shaped like
        //   { "ok": true, "errors": [] }
        Ok(r#"{"ok":true,"errors":[]}"#.to_string())
    }
}

export!(Component);
```

- [ ] **Step 4: wit/world.wit**

`crates/greentic-ext-cli/templates/design/wit/world.wit.tmpl`:

```wit
package {{id_wit}};

world extension {
  import greentic:extension-host/host@{{contract_version}};
  export greentic:extension-design/design@{{contract_version}};
}
```

- [ ] **Step 5: prompts/system.md**

`crates/greentic-ext-cli/templates/design/prompts/system.md.tmpl`:

```markdown
# {{name}} — system prompt

This file is loaded by designer UIs when this extension is active.
Edit to describe the authoring experience this extension provides.
```

- [ ] **Step 6: Commit**

```bash
git add crates/greentic-ext-cli/templates/design
git commit -m "feat(ext-cli): design kind templates (Cargo, describe, lib, wit, prompts)"
```

---

## Task 13: Bundle kind templates

**Files:**
- Create: `crates/greentic-ext-cli/templates/bundle/Cargo.toml.tmpl`
- Create: `crates/greentic-ext-cli/templates/bundle/describe.json.tmpl`
- Create: `crates/greentic-ext-cli/templates/bundle/src/lib.rs.tmpl`
- Create: `crates/greentic-ext-cli/templates/bundle/wit/world.wit.tmpl`

- [ ] **Step 1: Cargo.toml** — copy design's `Cargo.toml.tmpl` and swap the last `extension-design` dependency line for `extension-bundle`. Identical otherwise.

- [ ] **Step 2: describe.json** — copy design's `describe.json.tmpl` and change `"kind": "design"` → `"kind": "bundle"`, and contract string `extension-design` → `extension-bundle`.

- [ ] **Step 3: src/lib.rs**

```rust
// Bundle extension guest for {{id}}.
// Edit `list_recipes` and `build_pack` to assemble application packs.

wit_bindgen::generate!({
    world: "extension",
    path: "wit",
});

struct Component;

impl exports::greentic::extension_bundle::bundle::Guest for Component {
    fn list_recipes() -> Vec<exports::greentic::extension_bundle::bundle::Recipe> {
        vec![exports::greentic::extension_bundle::bundle::Recipe {
            id: "{{id}}.default".to_string(),
            title: "Default recipe".to_string(),
            description: "Edit list_recipes in src/lib.rs".to_string(),
        }]
    }

    fn build_pack(_recipe_id: String, _input: String) -> Result<Vec<u8>, String> {
        // TODO: implement. Return application pack bytes (ZIP/SquashFS).
        Ok(vec![])
    }
}

export!(Component);
```

- [ ] **Step 4: wit/world.wit**

```wit
package {{id_wit}};

world extension {
  import greentic:extension-host/host@{{contract_version}};
  export greentic:extension-bundle/bundle@{{contract_version}};
}
```

- [ ] **Step 5: Commit**

```bash
git add crates/greentic-ext-cli/templates/bundle
git commit -m "feat(ext-cli): bundle kind templates"
```

---

## Task 14: Deploy kind templates

**Files:**
- Create: `crates/greentic-ext-cli/templates/deploy/Cargo.toml.tmpl`
- Create: `crates/greentic-ext-cli/templates/deploy/describe.json.tmpl`
- Create: `crates/greentic-ext-cli/templates/deploy/src/lib.rs.tmpl`
- Create: `crates/greentic-ext-cli/templates/deploy/wit/world.wit.tmpl`

- [ ] **Step 1: Cargo.toml** — copy design's `Cargo.toml.tmpl` and swap `extension-design` → `extension-deploy`.

- [ ] **Step 2: describe.json** — kind `deploy`, contract `greentic:extension-deploy`.

- [ ] **Step 3: src/lib.rs**

```rust
// Deploy extension guest for {{id}}.
// Edit `plan` and `apply` to implement deployment lifecycle.

wit_bindgen::generate!({
    world: "extension",
    path: "wit",
});

struct Component;

impl exports::greentic::extension_deploy::deploy::Guest for Component {
    fn plan(_input: String) -> Result<String, String> {
        // TODO: return a JSON plan description.
        Ok(r#"{"actions":[]}"#.to_string())
    }

    fn apply(_plan_json: String) -> Result<String, String> {
        // TODO: execute the plan and return a JSON result.
        Ok(r#"{"ok":true}"#.to_string())
    }
}

export!(Component);
```

- [ ] **Step 4: wit/world.wit**

```wit
package {{id_wit}};

world extension {
  import greentic:extension-host/host@{{contract_version}};
  export greentic:extension-deploy/deploy@{{contract_version}};
}
```

- [ ] **Step 5: Commit**

```bash
git add crates/greentic-ext-cli/templates/deploy
git commit -m "feat(ext-cli): deploy kind templates"
```

---

## Task 15: Template loader — include_dir + resolution

**Files:**
- Modify: `crates/greentic-ext-cli/src/scaffold/template.rs`

- [ ] **Step 1: Add failing test**

Append inside `tests` module:

```rust
    #[test]
    fn load_common_returns_gitignore_template() {
        let entries = load_templates_common();
        assert!(entries.iter().any(|e| e.dst_rel == "gitignore.tmpl" || e.dst_rel == ".gitignore"));
    }

    #[test]
    fn load_kind_design_returns_cargo_toml() {
        let entries = load_templates_kind("design");
        assert!(entries.iter().any(|e| e.dst_rel == "Cargo.toml"));
        assert!(entries.iter().any(|e| e.dst_rel == "describe.json"));
        assert!(entries.iter().any(|e| e.dst_rel == "src/lib.rs"));
    }
```

- [ ] **Step 2: Implement**

Append to `template.rs` (above `#[cfg(test)]`):

```rust
use include_dir::{include_dir, Dir};

static TEMPLATES_COMMON: Dir<'_> = include_dir!("$CARGO_MANIFEST_DIR/templates/common");
static TEMPLATES_DESIGN: Dir<'_> = include_dir!("$CARGO_MANIFEST_DIR/templates/design");
static TEMPLATES_BUNDLE: Dir<'_> = include_dir!("$CARGO_MANIFEST_DIR/templates/bundle");
static TEMPLATES_DEPLOY: Dir<'_> = include_dir!("$CARGO_MANIFEST_DIR/templates/deploy");

#[derive(Debug, Clone)]
pub struct TemplateEntry {
    pub src_bytes: &'static [u8],
    /// Destination relative path inside the project (with `.tmpl` stripped and
    /// `gitignore` renamed to `.gitignore`).
    pub dst_rel: String,
}

fn collect(dir: &'static Dir<'static>) -> Vec<TemplateEntry> {
    let mut out = Vec::new();
    collect_rec(dir, &mut out);
    out
}

fn collect_rec(dir: &'static Dir<'static>, out: &mut Vec<TemplateEntry>) {
    for entry in dir.entries() {
        match entry {
            include_dir::DirEntry::File(f) => {
                let rel = f.path().to_string_lossy().to_string();
                let dst = translate_dst(&rel);
                out.push(TemplateEntry {
                    src_bytes: f.contents(),
                    dst_rel: dst,
                });
            }
            include_dir::DirEntry::Dir(d) => collect_rec(d, out),
        }
    }
}

fn translate_dst(rel: &str) -> String {
    let mut dst = rel.trim_end_matches(".tmpl").to_string();
    if dst == "gitignore" {
        dst = ".gitignore".to_string();
    }
    dst
}

pub fn load_templates_common() -> Vec<TemplateEntry> {
    collect(&TEMPLATES_COMMON)
}

pub fn load_templates_kind(kind: &str) -> Vec<TemplateEntry> {
    match kind {
        "design" => collect(&TEMPLATES_DESIGN),
        "bundle" => collect(&TEMPLATES_BUNDLE),
        "deploy" => collect(&TEMPLATES_DEPLOY),
        _ => Vec::new(),
    }
}
```

- [ ] **Step 3: Run tests, verify pass**

Run: `cargo test -p greentic-ext-cli --lib scaffold::template 2>&1 | tail -10`
Expected: 8 passed (4 previous + 2 new + 2 others).

- [ ] **Step 4: Commit**

```bash
git add crates/greentic-ext-cli/src/scaffold/template.rs
git commit -m "feat(ext-cli): template loader via include_dir for common + per-kind assets"
```

---

## Task 16: Orchestration — wire command end-to-end

**Files:**
- Modify: `crates/greentic-ext-cli/src/commands/new.rs`

- [ ] **Step 1: Replace stub `run` with full orchestration**

Replace the entire `run` function in `commands/new.rs`:

```rust
use std::{
    collections::BTreeMap,
    fs,
    path::{Path, PathBuf},
    time::SystemTime,
};

use clap::Args as ClapArgs;

use crate::scaffold::{
    contract_lock::ContractLock,
    embedded::{self, CONTRACT_VERSION},
    preflight::{self, Check},
    template::{self, Context},
    Kind,
};

// (Keep existing Args struct from Task 3. Only replace `pub fn run` body.)

pub fn run(args: &Args, _home: &Path) -> anyhow::Result<()> {
    // 1. Derive values
    let target = args
        .dir
        .clone()
        .unwrap_or_else(|| PathBuf::from(&args.name));
    let id = args
        .id
        .clone()
        .unwrap_or_else(|| format!("com.example.{}", args.name));
    let author = args.author.clone().unwrap_or_else(detect_git_author);
    validate_id(&id)?;
    validate_version(&args.version)?;

    // 2. Preflight
    let checks = vec![
        preflight::check_cargo_available(),
        preflight::check_cargo_component_available(),
        preflight::check_wasm32_wasip2_target(),
        preflight::check_target_dir(&target, args.force),
    ];
    print_checks(&checks);
    if checks.iter().any(|c| matches!(c, Check::Fail { .. })) {
        anyhow::bail!("preflight failed; fix the issues above and re-run");
    }

    // 3. If target exists with --force, clean it.
    if target.exists() && args.force {
        fs::remove_dir_all(&target)?;
    }
    fs::create_dir_all(&target)?;

    // 4. Render templates
    let mut ctx = Context::new();
    ctx.set("name", args.name.clone());
    ctx.set("kind", args.kind.as_str());
    ctx.set("id", &id);
    ctx.set("id_wit", id_to_wit_package(&id));
    ctx.set("version", &args.version);
    ctx.set("author", &author);
    ctx.set("license", &args.license);
    ctx.set("contract_version", CONTRACT_VERSION);

    let mut files_written = 0usize;
    for entry in template::load_templates_common() {
        let dst = target.join(&entry.dst_rel);
        let rendered = ctx.render(std::str::from_utf8(entry.src_bytes)?)?;
        template::write_file(&dst, rendered.as_bytes())?;
        files_written += 1;
    }
    for entry in template::load_templates_kind(args.kind.as_str()) {
        let dst = target.join(&entry.dst_rel);
        let rendered = ctx.render(std::str::from_utf8(entry.src_bytes)?)?;
        template::write_file(&dst, rendered.as_bytes())?;
        files_written += 1;
    }

    // 5. Write vendored WIT + contract lock
    let mut lock_files = BTreeMap::new();
    for file in embedded::files_for_kind(args.kind.as_str()) {
        let pkg_dir = wit_package_subdir_for(file.name);
        let dst = target
            .join("wit/deps/greentic")
            .join(pkg_dir)
            .join("world.wit");
        template::write_file(&dst, file.bytes)?;
        let rel = dst.strip_prefix(&target).unwrap().display().to_string();
        lock_files.insert(rel, format!("sha256:{}", embedded::sha256_hex(file.bytes)));
        files_written += 1;
    }
    let lock = ContractLock {
        contract_version: CONTRACT_VERSION.to_string(),
        generated_by: format!("gtdx {CONTRACT_VERSION}"),
        generated_at: now_iso8601(),
        files: lock_files,
    };
    template::write_file(&target.join(".gtdx-contract.lock"), lock.to_toml()?.as_bytes())?;
    files_written += 1;

    // 6. Make scripts executable (Unix only).
    #[cfg(unix)]
    {
        use std::os::unix::fs::PermissionsExt;
        for rel in ["build.sh", "ci/local_check.sh"] {
            let p = target.join(rel);
            if p.exists() {
                let mut perms = fs::metadata(&p)?.permissions();
                perms.set_mode(0o755);
                fs::set_permissions(&p, perms)?;
            }
        }
    }

    // 7. git init unless --no-git
    if !args.no_git {
        let _ = std::process::Command::new("git")
            .arg("init")
            .arg("--quiet")
            .current_dir(&target)
            .status();
    }

    println!();
    println!(
        "Scaffolded {} extension at {} ({} files, contract {}).",
        args.kind.as_str(),
        target.display(),
        files_written,
        CONTRACT_VERSION
    );
    println!();
    println!("Next steps:");
    println!("  cd {}", target.display());
    println!("  gtdx dev        # watch, rebuild, reinstall");
    println!("  gtdx publish    # pack to dist/");
    Ok(())
}

fn detect_git_author() -> String {
    std::process::Command::new("git")
        .args(["config", "--get", "user.name"])
        .output()
        .ok()
        .and_then(|o| {
            if o.status.success() {
                let s = String::from_utf8_lossy(&o.stdout).trim().to_string();
                if s.is_empty() { None } else { Some(s) }
            } else {
                None
            }
        })
        .unwrap_or_else(|| "Unknown".to_string())
}

fn validate_id(id: &str) -> anyhow::Result<()> {
    let re = regex_like(id);
    if !re {
        anyhow::bail!("id must match reverse-DNS (got {id:?})");
    }
    Ok(())
}

fn regex_like(id: &str) -> bool {
    // Reverse-DNS: [a-z][a-z0-9-]*(\.[a-z][a-z0-9-]*)+
    let parts: Vec<&str> = id.split('.').collect();
    if parts.len() < 2 {
        return false;
    }
    for p in parts {
        if p.is_empty() {
            return false;
        }
        let mut chars = p.chars();
        let first = chars.next().unwrap();
        if !first.is_ascii_lowercase() {
            return false;
        }
        if !chars.all(|c| c.is_ascii_lowercase() || c.is_ascii_digit() || c == '-') {
            return false;
        }
    }
    true
}

fn validate_version(version: &str) -> anyhow::Result<()> {
    semver::Version::parse(version)
        .map(|_| ())
        .map_err(|e| anyhow::anyhow!("version {version:?} is not valid semver: {e}"))
}

fn id_to_wit_package(id: &str) -> String {
    // com.example.demo -> com-example:demo
    let mut parts: Vec<&str> = id.split('.').collect();
    let last = parts.pop().unwrap_or("ext");
    format!("{}:{}", parts.join("-"), last)
}

fn wit_package_subdir_for(filename: &str) -> &'static str {
    match filename {
        "extension-base.wit" => "extension-base",
        "extension-host.wit" => "extension-host",
        "extension-design.wit" => "extension-design",
        "extension-bundle.wit" => "extension-bundle",
        "extension-deploy.wit" => "extension-deploy",
        "runtime-side.wit" => "runtime-side",
        _ => "extension-misc",
    }
}

fn now_iso8601() -> String {
    let secs = SystemTime::now()
        .duration_since(SystemTime::UNIX_EPOCH)
        .map(|d| d.as_secs())
        .unwrap_or(0);
    // Very minimal ISO8601 (UTC, no tz offset). Good enough for a lock timestamp.
    let d = chrono_like_date(secs);
    format!(
        "{:04}-{:02}-{:02}T{:02}:{:02}:{:02}Z",
        d.year, d.month, d.day, d.hour, d.minute, d.second
    )
}

struct DateParts {
    year: u32,
    month: u32,
    day: u32,
    hour: u32,
    minute: u32,
    second: u32,
}

fn chrono_like_date(mut secs: u64) -> DateParts {
    let second = (secs % 60) as u32;
    secs /= 60;
    let minute = (secs % 60) as u32;
    secs /= 60;
    let hour = (secs % 24) as u32;
    secs /= 24;
    // days since 1970-01-01 → Y-M-D via civil algorithm (Howard Hinnant).
    let mut days = secs as i64;
    days += 719468;
    let era = if days >= 0 { days / 146097 } else { (days - 146096) / 146097 };
    let doe = (days - era * 146097) as u64;
    let yoe = (doe - doe / 1460 + doe / 36524 - doe / 146096) / 365;
    let y = yoe as i64 + era * 400;
    let doy = doe - (365 * yoe + yoe / 4 - yoe / 100);
    let mp = (5 * doy + 2) / 153;
    let d = doy - (153 * mp + 2) / 5 + 1;
    let m = if mp < 10 { mp + 3 } else { mp - 9 };
    let year = (y + if m <= 2 { 1 } else { 0 }) as u32;
    DateParts {
        year,
        month: m as u32,
        day: d as u32,
        hour,
        minute,
        second,
    }
}

fn print_checks(checks: &[Check]) {
    for c in checks {
        match c {
            Check::Pass { name, detail } => println!("  ✓ {name}: {detail}"),
            Check::Warn { name, hint } => println!("  ! {name}: {hint}"),
            Check::Fail { name, hint } => eprintln!("  ✗ {name}: {hint}"),
        }
    }
}
```

- [ ] **Step 2: Verify compiles**

Run: `cargo build -p greentic-ext-cli 2>&1 | tail -20`
Expected: compiles cleanly (no warnings under `-D warnings` with clippy workspace lints).

- [ ] **Step 3: Smoke-run**

Run:
```bash
rm -rf /tmp/gtdx-new-smoke && ./target/debug/gtdx new /tmp/gtdx-new-smoke/demo --dir /tmp/gtdx-new-smoke/demo -y
ls /tmp/gtdx-new-smoke/demo
cat /tmp/gtdx-new-smoke/demo/.gtdx-contract.lock
```
Expected: files listed include `Cargo.toml`, `describe.json`, `src/lib.rs`, `wit/`, `.gtdx-contract.lock`. Lock file TOML shows 3 `wit/deps/greentic/extension-*/world.wit` entries with SHA256 hashes.

- [ ] **Step 4: Commit**

```bash
git add crates/greentic-ext-cli/src/commands/new.rs
git commit -m "feat(ext-cli): wire gtdx new orchestration (preflight + render + vendor WIT + lock)"
```

---

## Task 17: Integration test — design ext scaffolds and cargo-check succeeds

**Files:**
- Create: `crates/greentic-ext-cli/tests/cli_new.rs`

- [ ] **Step 1: Write failing test**

`crates/greentic-ext-cli/tests/cli_new.rs`:

```rust
use std::process::Command;

fn gtdx_bin() -> std::path::PathBuf {
    // assert_cmd::cargo::cargo_bin would also work; using env! is simpler.
    let mut p = std::path::PathBuf::from(env!("CARGO_MANIFEST_DIR"));
    p.pop();
    p.pop();
    p.push("target/debug/gtdx");
    p
}

fn run(cmd: &mut Command) -> (bool, String, String) {
    let out = cmd.output().expect("spawn gtdx");
    (
        out.status.success(),
        String::from_utf8_lossy(&out.stdout).to_string(),
        String::from_utf8_lossy(&out.stderr).to_string(),
    )
}

#[test]
fn scaffolds_design_extension_and_lock_file_matches_bytes() {
    let tmp = tempfile::tempdir().unwrap();
    let proj = tmp.path().join("demo");
    let (ok, stdout, stderr) = run(Command::new(gtdx_bin())
        .arg("new")
        .arg("demo")
        .arg("--dir")
        .arg(&proj)
        .arg("-y")
        .arg("--no-git"));
    assert!(ok, "gtdx new failed\nstdout:\n{stdout}\nstderr:\n{stderr}");

    for rel in [
        "Cargo.toml",
        "describe.json",
        "src/lib.rs",
        ".gtdx-contract.lock",
        "wit/deps/greentic/extension-base/world.wit",
        "wit/deps/greentic/extension-host/world.wit",
        "wit/deps/greentic/extension-design/world.wit",
    ] {
        assert!(
            proj.join(rel).exists(),
            "missing expected file: {rel}\nstdout:\n{stdout}"
        );
    }

    let lock = std::fs::read_to_string(proj.join(".gtdx-contract.lock")).unwrap();
    assert!(lock.contains("contract_version"));
    assert!(lock.contains("wit/deps/greentic/extension-base/world.wit"));

    // Verify hash in lock matches actual bytes on disk.
    let base_bytes = std::fs::read(proj.join("wit/deps/greentic/extension-base/world.wit")).unwrap();
    let expected_sha = {
        use sha2::{Digest, Sha256};
        let d = Sha256::digest(&base_bytes);
        let mut s = String::new();
        for b in d {
            use std::fmt::Write as _;
            write!(&mut s, "{b:02x}").unwrap();
        }
        s
    };
    assert!(
        lock.contains(&format!("sha256:{expected_sha}")),
        "lock file hash did not match on-disk WIT bytes"
    );
}
```

- [ ] **Step 2: Build gtdx, run test**

Run:
```bash
cargo build -p greentic-ext-cli
cargo test -p greentic-ext-cli --test cli_new 2>&1 | tail -10
```
Expected: 1 passed.

- [ ] **Step 3: Commit**

```bash
git add crates/greentic-ext-cli/tests/cli_new.rs
git commit -m "test(ext-cli): integration test asserting design scaffold layout + lock-file hash"
```

---

## Task 18: Integration test — bundle + deploy kinds

**Files:**
- Modify: `crates/greentic-ext-cli/tests/cli_new.rs`

- [ ] **Step 1: Add tests**

Append to `tests/cli_new.rs`:

```rust
#[test]
fn scaffolds_bundle_extension_with_correct_wit_deps() {
    let tmp = tempfile::tempdir().unwrap();
    let proj = tmp.path().join("b");
    let (ok, _o, e) = run(Command::new(gtdx_bin())
        .arg("new")
        .arg("b")
        .arg("--kind")
        .arg("bundle")
        .arg("--dir")
        .arg(&proj)
        .arg("-y")
        .arg("--no-git"));
    assert!(ok, "gtdx new bundle failed: {e}");
    assert!(proj.join("wit/deps/greentic/extension-bundle/world.wit").exists());
    assert!(!proj.join("wit/deps/greentic/extension-design/world.wit").exists());
    let describe = std::fs::read_to_string(proj.join("describe.json")).unwrap();
    assert!(describe.contains("\"kind\": \"bundle\""));
}

#[test]
fn scaffolds_deploy_extension_with_correct_wit_deps() {
    let tmp = tempfile::tempdir().unwrap();
    let proj = tmp.path().join("d");
    let (ok, _o, e) = run(Command::new(gtdx_bin())
        .arg("new")
        .arg("d")
        .arg("--kind")
        .arg("deploy")
        .arg("--dir")
        .arg(&proj)
        .arg("-y")
        .arg("--no-git"));
    assert!(ok, "gtdx new deploy failed: {e}");
    assert!(proj.join("wit/deps/greentic/extension-deploy/world.wit").exists());
    assert!(!proj.join("wit/deps/greentic/extension-bundle/world.wit").exists());
    let describe = std::fs::read_to_string(proj.join("describe.json")).unwrap();
    assert!(describe.contains("\"kind\": \"deploy\""));
}
```

- [ ] **Step 2: Run tests, verify pass**

Run: `cargo test -p greentic-ext-cli --test cli_new 2>&1 | tail -10`
Expected: 3 passed.

- [ ] **Step 3: Commit**

```bash
git add crates/greentic-ext-cli/tests/cli_new.rs
git commit -m "test(ext-cli): integration coverage for bundle + deploy kind scaffolds"
```

---

## Task 19: Integration test — target dir conflict + --force behavior

**Files:**
- Modify: `crates/greentic-ext-cli/tests/cli_new.rs`

- [ ] **Step 1: Add tests**

Append to `tests/cli_new.rs`:

```rust
#[test]
fn target_dir_conflict_without_force_fails() {
    let tmp = tempfile::tempdir().unwrap();
    let proj = tmp.path().join("demo");
    std::fs::create_dir_all(&proj).unwrap();
    std::fs::write(proj.join("something"), "x").unwrap();

    let (ok, _o, e) = run(Command::new(gtdx_bin())
        .arg("new")
        .arg("demo")
        .arg("--dir")
        .arg(&proj)
        .arg("-y")
        .arg("--no-git"));
    assert!(!ok);
    assert!(e.contains("--force") || e.contains("already exists"), "stderr:\n{e}");

    // Pre-existing file must remain untouched.
    let kept = std::fs::read_to_string(proj.join("something")).unwrap();
    assert_eq!(kept, "x");
}

#[test]
fn target_dir_conflict_with_force_succeeds() {
    let tmp = tempfile::tempdir().unwrap();
    let proj = tmp.path().join("demo");
    std::fs::create_dir_all(&proj).unwrap();
    std::fs::write(proj.join("something"), "x").unwrap();

    let (ok, _o, e) = run(Command::new(gtdx_bin())
        .arg("new")
        .arg("demo")
        .arg("--dir")
        .arg(&proj)
        .arg("--force")
        .arg("-y")
        .arg("--no-git"));
    assert!(ok, "stderr:\n{e}");
    assert!(!proj.join("something").exists(), "old file should be gone after --force");
    assert!(proj.join("Cargo.toml").exists());
}
```

- [ ] **Step 2: Run tests, verify pass**

Run: `cargo test -p greentic-ext-cli --test cli_new 2>&1 | tail -10`
Expected: 5 passed.

- [ ] **Step 3: Commit**

```bash
git add crates/greentic-ext-cli/tests/cli_new.rs
git commit -m "test(ext-cli): target-dir conflict handling and --force overwrite semantics"
```

---

## Task 20: Integration test — `cargo check` on generated design project

**Files:**
- Modify: `crates/greentic-ext-cli/tests/cli_new.rs`

- [ ] **Step 1: Add failing test (gated behind env var)**

Append to `tests/cli_new.rs`:

```rust
/// Slow smoke test: generate a project and confirm `cargo check --quiet`
/// succeeds. Gated behind GTDX_RUN_CARGO_CHECK=1 because it needs network
/// for dep resolution (unless an offline lockfile exists).
#[test]
fn generated_project_passes_cargo_check() {
    if std::env::var("GTDX_RUN_CARGO_CHECK").ok().as_deref() != Some("1") {
        eprintln!("skip: set GTDX_RUN_CARGO_CHECK=1 to run this test");
        return;
    }
    let tmp = tempfile::tempdir().unwrap();
    let proj = tmp.path().join("demo");
    let (ok, _o, e) = run(Command::new(gtdx_bin())
        .arg("new")
        .arg("demo")
        .arg("--dir")
        .arg(&proj)
        .arg("-y")
        .arg("--no-git"));
    assert!(ok, "gtdx new failed: {e}");

    let (ok, stdout, stderr) = run(Command::new("cargo")
        .arg("check")
        .arg("--quiet")
        .current_dir(&proj));
    assert!(ok, "cargo check failed\nstdout:\n{stdout}\nstderr:\n{stderr}");
}
```

- [ ] **Step 2: Run with env var set**

Run: `GTDX_RUN_CARGO_CHECK=1 cargo test -p greentic-ext-cli --test cli_new generated_project_passes_cargo_check -- --nocapture 2>&1 | tail -40`
Expected: passes (may take 1-3 minutes on first run to resolve `wit-bindgen-rt` + `cargo-component` target deps).

If it fails due to `cargo-component` not invoked (it's `cargo check` not `cargo component check`): accept this as known — the generated project compiles as a Rust lib; full component build is verified in Task 21.

- [ ] **Step 3: Commit**

```bash
git add crates/greentic-ext-cli/tests/cli_new.rs
git commit -m "test(ext-cli): optional cargo check smoke test on generated project"
```

---

## Task 21: Acceptance gate — rescaffold AC + bundle-standard, diff check

**Files:**
- Create: `crates/greentic-ext-cli/tests/rescaffold_reference_exts.rs`

- [ ] **Step 1: Write acceptance gate (manual-run test)**

`crates/greentic-ext-cli/tests/rescaffold_reference_exts.rs`:

```rust
//! Acceptance gate for Phase 1 Track A spec §11 criterion 6:
//! "Both reference extensions (AC design ext, bundle-standard) can be
//!  rescaffolded via `gtdx new` and diffed ≤ 5 line changes vs generated
//!  output (acceptable customization, not a rewrite)."
//!
//! This test is gated behind GTDX_RUN_ACCEPTANCE=1 and expects:
//! - AC_EXT_PATH env var pointing to greentic-adaptive-card-mcp/crates/adaptive-card-extension
//! - BUNDLE_STD_PATH env var pointing to greentic-bundle-extensions/bundle-standard

use std::path::PathBuf;
use std::process::Command;

fn gtdx_bin() -> PathBuf {
    let mut p = PathBuf::from(env!("CARGO_MANIFEST_DIR"));
    p.pop();
    p.pop();
    p.push("target/debug/gtdx");
    p
}

fn run(cmd: &mut Command) -> (bool, String) {
    let out = cmd.output().expect("spawn");
    (out.status.success(), String::from_utf8_lossy(&out.stderr).to_string())
}

#[test]
fn rescaffold_ac_extension_layout_overlap() {
    if std::env::var("GTDX_RUN_ACCEPTANCE").ok().as_deref() != Some("1") {
        eprintln!("skip: set GTDX_RUN_ACCEPTANCE=1 + AC_EXT_PATH");
        return;
    }
    let ac_path = std::env::var("AC_EXT_PATH").expect("AC_EXT_PATH");
    let tmp = tempfile::tempdir().unwrap();
    let proj = tmp.path().join("ac-rescaffolded");
    let (ok, e) = run(Command::new(gtdx_bin())
        .arg("new")
        .arg("adaptive-cards")
        .arg("--kind")
        .arg("design")
        .arg("--id")
        .arg("greentic.adaptive-cards")
        .arg("--dir")
        .arg(&proj)
        .arg("-y")
        .arg("--no-git"));
    assert!(ok, "scaffold: {e}");

    // Sanity: generated project must share top-level shape with AC ext.
    for rel in ["Cargo.toml", "describe.json", "src/lib.rs", "wit"] {
        assert!(proj.join(rel).exists(), "generated missing {rel}");
        assert!(
            std::path::Path::new(&ac_path).join(rel).exists(),
            "AC ext missing {rel} at {ac_path}"
        );
    }
}

#[test]
fn rescaffold_bundle_standard_layout_overlap() {
    if std::env::var("GTDX_RUN_ACCEPTANCE").ok().as_deref() != Some("1") {
        eprintln!("skip: set GTDX_RUN_ACCEPTANCE=1 + BUNDLE_STD_PATH");
        return;
    }
    let std_path = std::env::var("BUNDLE_STD_PATH").expect("BUNDLE_STD_PATH");
    let tmp = tempfile::tempdir().unwrap();
    let proj = tmp.path().join("bundle-rescaffolded");
    let (ok, e) = run(Command::new(gtdx_bin())
        .arg("new")
        .arg("bundle-standard")
        .arg("--kind")
        .arg("bundle")
        .arg("--dir")
        .arg(&proj)
        .arg("-y")
        .arg("--no-git"));
    assert!(ok, "scaffold: {e}");

    for rel in ["Cargo.toml", "describe.json", "src/lib.rs", "wit"] {
        assert!(proj.join(rel).exists(), "generated missing {rel}");
        assert!(
            std::path::Path::new(&std_path).join(rel).exists(),
            "bundle-standard missing {rel} at {std_path}"
        );
    }
}
```

The deep ≤-5-line diff assertion is done manually after the author runs the test and inspects `diff -r` output. The programmatic test asserts the **shape** matches; the author confirms customization scope separately and records it in the Phase 1 release notes.

- [ ] **Step 2: Run with env (author machine only)**

Run:
```bash
GTDX_RUN_ACCEPTANCE=1 \
  AC_EXT_PATH=/home/bimbim/works/greentic/greentic-adaptive-card-mcp/crates/adaptive-card-extension \
  BUNDLE_STD_PATH=/home/bimbim/works/greentic/greentic-bundle-extensions/bundle-standard \
  cargo test -p greentic-ext-cli --test rescaffold_reference_exts -- --nocapture 2>&1 | tail -20
```
Expected: 2 passed.

- [ ] **Step 3: Commit**

```bash
git add crates/greentic-ext-cli/tests/rescaffold_reference_exts.rs
git commit -m "test(ext-cli): acceptance gate for rescaffolding AC + bundle-standard reference exts"
```

---

## Task 22: Docs + CHANGELOG + repo-level CLAUDE.md note

**Files:**
- Create: `docs/getting-started-scaffolding.md`
- Modify: `docs/cli-reference.md` (if exists; else skip this step)
- Modify: `CHANGELOG.md` (create if missing)

- [ ] **Step 1: Write getting-started**

`docs/getting-started-scaffolding.md`:

```markdown
# Scaffolding a new extension

`gtdx new` generates a ready-to-build Greentic Designer Extension project.

## Prerequisites

- Rust 1.94+ with `wasm32-wasip2` target: `rustup target add wasm32-wasip2`
- `cargo-component`: `cargo install --locked cargo-component`

## Create

```
gtdx new my-ext --kind design --id com.example.my-ext
```

Supported kinds: `design`, `bundle`, `deploy`.

## What you get

```
my-ext/
├── .gtdx-contract.lock     WIT contract version + file hashes
├── Cargo.toml
├── README.md
├── build.sh
├── ci/local_check.sh
├── describe.json
├── i18n/en.json
├── src/lib.rs              WASM guest exports
└── wit/deps/greentic/...   Vendored WIT contract
```

## Next

```
cd my-ext
gtdx dev        # continuous rebuild + install (Track B, Phase 1)
gtdx publish    # produce and publish a .gtxpack (Track C, Phase 1)
```

See also: `docs/how-to-write-a-design-extension.md`.
```

- [ ] **Step 2: Add CHANGELOG entry (top)**

Create or edit `CHANGELOG.md`; add:

```markdown
## Unreleased

### Added
- `gtdx new <name>` — scaffold a new design/bundle/deploy extension with
  vendored WIT contract and `.gtdx-contract.lock` (Phase 1 Track A, S1).
```

- [ ] **Step 3: Commit**

```bash
git add docs/getting-started-scaffolding.md CHANGELOG.md
git commit -m "docs(ext-cli): getting-started for gtdx new + CHANGELOG entry"
```

---

## Task 23: Format, clippy, full test suite — final green gate

- [ ] **Step 1: Format**

Run: `cargo fmt --all`
Expected: no output (idempotent).

- [ ] **Step 2: Clippy**

Run: `cargo clippy --workspace --all-targets -- -D warnings 2>&1 | tail -20`
Expected: exit 0, no warnings.

- [ ] **Step 3: Full test suite**

Run: `cargo test --workspace --all-targets 2>&1 | tail -30`
Expected: all existing + new tests green.

- [ ] **Step 4: Commit any formatting fixes**

```bash
if ! git diff --quiet; then
  git add -A
  git commit -m "style(ext-cli): cargo fmt post-feature"
fi
```

- [ ] **Step 5: Push branch**

```bash
git push -u origin spec/dx-10-10-roadmap
```

---

## Acceptance (Phase 1 Track A complete when all true)

1. `gtdx new demo` (all defaults) produces a project passing `cargo check` (manual verify; Task 20 when GTDX_RUN_CARGO_CHECK=1).
2. `gtdx new demo --kind bundle` and `--kind deploy` produce compilable projects.
3. `.gtdx-contract.lock` hashes match actual vendored WIT bytes (Task 17).
4. Preflight errors are actionable with suggested next command (covered in Task 8/9).
5. Re-run in non-empty dir without `--force` fails, leaves dir untouched (Task 19).
6. `--force` overwrites cleanly (Task 19).
7. Reference extensions (AC + bundle-standard) rescaffold shape matches (Task 21, author-run).
8. Full workspace clippy+test green on Linux (Task 23). macOS/Windows CI matrix is configured in the repo's existing workflow; author verifies PR CI is green before merge.

---

## Self-Review

**1. Spec coverage:** Every acceptance criterion in sub-spec §11 maps to a task: A1→Task17, A2→Task18, A3→Task17, A4→Task8/9, A5→Task19, A6→Task21. ✓

**2. Placeholder scan:** No TBD/TODO/"handle edge cases" placeholders remain. Inline `// TODO: implement` comments exist in generated template code (that's where users write their logic — intentional). ✓

**3. Type consistency:** `Kind` enum used consistently between `scaffold/mod.rs` and `commands/new.rs`. `Check` variants (`Pass`/`Warn`/`Fail`) consistent across preflight checks. `ContractLock` struct + `to_toml()` signature consistent from Task 6 to Task 16. `TemplateEntry.dst_rel` field used consistently in Tasks 10/15/16. ✓

**4. Fix notes applied inline:**
- Task 16 uses `env!("CARGO_PKG_VERSION")` via `CONTRACT_VERSION` constant — matches build.rs version used by Task 2.
- Task 20 is explicitly gated to avoid flaky CI due to network/registry dependency.
- Task 21 programmatic assertion kept to shape-check only; deep diff recorded manually in release notes (realistic).

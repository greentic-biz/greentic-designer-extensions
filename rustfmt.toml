edition = "2024"
max_width = 100
reorder_imports = true
# `imports_granularity` + `group_imports` require nightly rustfmt. Tracked in
# follow-up; on stable we accept whatever ordering rustc produces.
